Drop project images here using the filenames referenced in _build/projects.json.
Missing images degrade gracefully — the page shows a labelled placeholder instead
of a broken image icon, so you can add them one at a time.
